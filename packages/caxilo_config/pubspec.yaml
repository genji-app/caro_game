name: caxilo_config
description: A standalone package for managing Caxilo in-house game configuration and launching.
version: 3.6.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.10.1
  freezed: ^3.2.3
  json_serializable: ^6.8.0
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
