// Dart-only export for use in CLI tools (no Flutter / dart:ui dependency).
// Import this instead of caxilo_config.dart when running with plain `dart run`.
export 'src/presets/caxilo_config_presets.dart';
